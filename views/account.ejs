<!DOCTYPE html>
<html>
  <head>
    <title>Members Only</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link href="/stylesheets/output.css" rel="stylesheet" />
    <script src="/javascripts/account.js" defer></script>
  </head>
  <body class="p-0 text-slate-700 bg-slate-100">
    <% if (typeof user !== `undefined`) {%>

    <nav
      class="px-4 py-6 gap-x-4 md:gap-x-6 md:px-6 w-full h-fit flex flex-row justify-evenly md:justify-end flex-initial bg-slate-500 text-white text-sm"
    >
      <a href="/posts">Home</a>
      <div class="border border-white border-l md:hidden"></div>
      <a href="/posts/create">Post</a>
      <div class="border border-white border-l md:hidden"></div>
      <a href="/account">Account</a>
      <div class="border border-white border-l md:hidden"></div>
      <a class="whitespace-nowrap" href="/log-out">Log Out</a>
    </nav>

    <div
      class="p-6 md:px-8 py-8 md:py-16 gap-y-8 md:gap-y-16 w-full h-full flex flex-col justify-start items-center bg-slate-100"
    >
      <h1 class="text-center font-bold text-2xl md:text-3xl">
        Account Details
      </h1>
      <section
        class="md:min-w-[500px] min-w-full p-6 md:p-8 bg-white flex flex-col gap-y-6 justify-center items-center text-sm drop-shadow-lg rounded-lg"
      >
        <div class="flex flex-col gap-y-6 w-full">
          <div class="flex flex-col gap-y-2">
            <label for="email">Email</label>
            <input
              class="px-4 py-2 border border-slate-500 rounded-lg"
              required
              autocomplete="off"
              name="email"
              placeholder="Email"
              type="email"
              value="<%= typeof user.email === `undefined` ? `` : user.email %>"
              disabled
            />
          </div>

          <form
            class="relative flex flex-col gap-y-2"
            action="/update/first-name"
            method="post"
          >
            <label for="first_name">First Name</label>
            <div class="flex flex-row justify-between gap-x-4">
              <input
                id="first_name"
                class="w-full px-4 py-2 border border-slate-500 rounded-lg"
                required
                autocomplete="off"
                name="first_name"
                placeholder="First Name"
                type="text"
                value="<%= typeof user.first_name === `undefined` ? `` : user.first_name %>"
              />
              <button
                disabled="false"
                id="first-name-submit"
                class="disabled:pointer-events-none disabled:bg-slate-100 disabled:text-slate-500 px-4 py-2 rounded-lg border border-slate-500 bg-slate-500 text-white"
                type="submit"
              >
                Save
              </button>
            </div>
          </form>

          <form
            class="relative flex flex-col gap-y-2"
            action="/update/last-name"
            method="post"
          >
            <label for="last_name">Last Name</label>
            <div class="flex flex-row justify-between gap-x-4">
              <input
                id="last_name"
                class="w-full px-4 py-2 border border-slate-500 rounded-lg"
                required
                autocomplete="off"
                name="last_name"
                placeholder="Last Name"
                type="text"
                value="<%= typeof user.last_name === `undefined` ? `` : user.last_name %>"
              />
              <button
                disabled="true"
                id="last-name-submit"
                class="disabled:pointer-events-none disabled:bg-slate-100 disabled:text-slate-500 px-4 py-2 rounded-lg border border-slate-500 bg-slate-500 text-white"
                type="submit"
              >
                Save
              </button>
            </div>
          </form>

          <form
            action="/member"
            method="post"
            class="relative flex flex-col gap-y-2"
          >
            <div class="w-full flex flex-row justify-between items-center">
              <label for="member">Member:</label>
              <img
                id="member-toggle"
                class="w-12 h-auto cursor-pointer"
                src="<%= user.member ? `/images/toggle.svg` : `/images/toggle-off.svg` %>"
              />
            </div>

            <div
              class="w-full gap-x-4 flex flex-row justify-between items-center"
            >
              <input
                value="<%= user.member  %>"
                class="hidden"
                id="member"
                name="member"
                type="text"
              />

              <div
                class="hidden w-full flex flex-col"
                id="member-password-container"
              >
                <label class="hidden" for="member-password"
                  >Member Password:
                </label>
                <input
                  class="w-full px-4 py-2 border border-slate-500 rounded-lg"
                  id="member-password"
                  name="member-password"
                  placeholder="Member Password"
                  type="text"
                />
              </div>

              <button
                class="hidden disabled:pointer-events-none disabled:bg-slate-100 disabled:text-slate-500 px-4 py-2 rounded-lg border border-slate-500 bg-slate-500 text-white"
                id="member-submit"
                type="submit"
              >
                Save
              </button>
            </div>
          </form>

          <form
            action="/admin"
            method="post"
            class="relative flex flex-col gap-y-2"
          >
            <div class="w-full flex flex-row justify-between items-center">
              <label for="admin">Admin:</label>
              <img
                id="admin-toggle"
                class="w-12 h-auto cursor-pointer"
                src="<%= user.admin ? `/images/toggle.svg` : `/images/toggle-off.svg` %>"
              />
            </div>

            <div
              class="w-full gap-x-4 flex flex-row justify-between items-center"
            >
              <input
                value="<%= user.admin  %>"
                class="hidden"
                id="admin"
                name="admin"
                type="text"
              />

              <div
                class="hidden w-full flex flex-col"
                id="admin-password-container"
              >
                <label class="hidden" for="admin-password"
                  >Admin Password:
                </label>
                <input
                  class="w-full px-4 py-2 border border-slate-500 rounded-lg"
                  id="admin-password"
                  name="admin-password"
                  placeholder="Admin Password"
                  type="text"
                />
              </div>

              <button
                class="hidden disabled:pointer-events-none disabled:bg-slate-100 disabled:text-slate-500 px-4 py-2 rounded-lg border border-slate-500 bg-slate-500 text-white"
                id="admin-submit"
                type="submit"
              >
                Save
              </button>
            </div>
          </form>
        </div>
      </section>
    </div>

    <div class="hidden">
      <button id="password-toggle">Change Password</button>
      <form id="change-password-form" class="hidden">
        <div class="flex items-center gap-x-4">
          <label for="password">Old Password</label>
          <input required autocomplete="off" name="password" type="password" />
        </div>

        <div class="flex items-center gap-x-4">
          <label for="new-password">New Password</label>
          <input
            required
            autocomplete="off"
            name="new-password"
            type="password"
          />
        </div>

        <div class="flex items-center gap-x-4">
          <label for="confirm">Confirm Password</label>
          <input required autocomplete="off" name="confirm" type="password" />
        </div>
        <button type="submit">Save</button>
      </form>

      <form
        action="/admin"
        method="post"
        class="<%= user.member ? `` : `hidden` %> flex items-center gap-x-4"
      >
        <label for="admin">Admin:</label>
        <input
          value="<%= user.admin  %>"
          class="hidden"
          id="admin"
          name="admin"
          type="text"
        />
        <img
          id="admin-toggle"
          class="w-12 h-auto cursor-pointer"
          src="<%= user.admin ? `/images/toggle.svg` : `/images/toggle-off.svg` %>"
        />
        <div class="hidden" id="admin-password-container">
          <label for="admin-password">Admin Password: </label>
          <input id="admin-password" name="admin-password" type="text" />
        </div>

        <button id="admin-submit" class="hidden" type="submit">Save</button>
      </form>
    </div>

    <% } else { %>
    <div
      class="p-6 md:px-8 py-8 md:py-16 gap-y-8 md:gap-y-16 w-full h-full flex flex-col justify-start items-center bg-slate-100"
    >
      <h1 class="text-center font-bold text-2xl md:text-3xl">
        Members Only Forum
      </h1>
      <section
        class="md:min-w-[500px] min-w-full p-6 md:p-8 bg-white flex flex-col gap-y-6 justify-center items-center text-sm drop-shadow-lg rounded-lg"
      >
        <h2 class="text-xl md:text-2xl">Log In</h2>
        <form
          action="/log-in"
          method="POST"
          class="flex flex-col gap-y-6 w-full"
        >
          <div class="flex flex-col gap-y-2">
            <label for="email">Email</label>
            <input
              autocomplete="off"
              name="email"
              placeholder="Email"
              type="text"
              class="px-4 py-2 border border-slate-500 rounded-lg focus:bg-white"
            />
          </div>

          <div class="relative flex flex-col gap-y-2">
            <label for="password">Password</label>
            <input
              id="password"
              name="password"
              type="password"
              class="px-4 py-2 border border-slate-500 rounded-lg"
            />
            <img
              id="toggle-pw"
              class="absolute bottom-1.5 right-2 w-6 cursor-pointer"
              src="/images/eye.svg"
            />
          </div>

          <button
            class="px-4 py-2 border border-slate-500 rounded-lg bg-slate-500 text-white hover:bg-white hover:text-slate-700 transition"
          >
            Log In
          </button>
        </form>

        <div class="w-full border-slate-500 border-t"></div>

        <div class="flex flex-row items-center gap-x-2">
          <p>Don't have an account?</p>
          <a class="hover:underline" href="/sign-up">Sign Up</a>
        </div>
      </section>
    </div>
    <%}%>
  </body>
</html>
